name: CI
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sagasmith-dnd
    steps:
      - uses: actions/checkout@v4
        with:
          path: sagasmith-dnd
      - uses: actions/checkout@v4
        with:
          repository: SagaSmithAI/sagasmith-core
          path: sagasmith-core
      - uses: actions/checkout@v4
        with:
          repository: SagaSmithAI/SagaSmith-dnd-skills
          path: SagaSmith-dnd-skills
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: |
            sagasmith-dnd/pyproject.toml
            sagasmith-core/pyproject.toml
      - run: python -m pip install -e '../sagasmith-core[dev]' -e '.[dev]'
      - run: ruff check .
      - run: pytest
